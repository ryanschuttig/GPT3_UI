import React from "react";
import ReactDOM from "react-dom";

import App from "./App";

// render app inside div with ID of "root" index.html
ReactDOM.render(<App />, document.getElementById("root"));
